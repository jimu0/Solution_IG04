//using cfg;
//using Luban;

namespace IGC.RPGCore_IG04;

public static class DeckFactory
{
    //public static Func<string, ByteBuf>? Loader;

    //public static Tables? tables;// = new(file => Loader == null ? throw new Exception("DeckFactory.Loader not set") : Loader(file));
}
